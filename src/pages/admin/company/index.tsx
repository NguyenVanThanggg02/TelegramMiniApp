import React from "react";
import { Page } from "zmp-ui";

// Define the component with React.FC to type it as a functional component
const CompanyPage: React.FC = () => {
  return (
    <Page className="page">
      <div className="section-container">CompanyPage</div>
    </Page>
  );
};

export default CompanyPage;
